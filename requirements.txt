# Core dependencies
typer>=0.9
rich>=13.0

# Optional extras
# pip install vanish[tui] for interactive mode
# pip install vanish[trash] for OS trash integration
# pip install vanish[notifications] for Windows notifications

# Development dependencies
pytest>=7.0
pytest-cov>=4.0
black>=23.0
flake8>=6.0
mypy>=1.0
